MESSAGE(STATUS "        Compiling hydrology_subdir: PIHM...")
SET(MODNAME PIHM)
SET(ABSOLUTE_PATH_TO_SUNDIALS_CVODE_LIB "G:\\program\\seims\\SEIMS_C\\seims\\src\\cvode\\instdir\\lib\\sundials_cvode.lib")
SET(ABSOLUTE_PATH_TO_SUNDIALS_NVECSERIAL_LIB "G:\\program\\seims\\SEIMS_C\\seims\\src\\cvode\\instdir\\lib\\sundials_nvecserial.lib")
PROJECT(SEIMS_MODULE_${MODNAME})
LINK_DIRECTORIES(G:/program/seims/SEIMS_C/seims/src/cvode/instdir/lib)
FILE(GLOB SRC_LIST *.cpp *.h)
ADD_LIBRARY(${MODNAME} SHARED ${SRC_LIST})
SET(LIBRARY_OUTPUT_PATH ${SEIMS_BINARY_OUTPUT_PATH})
include_directories(G:/program/seims/SEIMS_C/seims/src/cvode/instdir/include)
TARGET_LINK_LIBRARIES(${MODNAME} module_setting ${ABSOLUTE_PATH_TO_SUNDIALS_CVODE_LIB} ${ABSOLUTE_PATH_TO_SUNDIALS_NVECSERIAL_LIB})
### For Xcode generator, specific LLVM-Clang installed by brew, and add link library of OpenMP explicitly.
IF(XCODE AND LLVM_FOUND)
    TARGET_LINK_LIBRARIES(${MODNAME} ${OpenMP_LIBRARY})
    SET_TARGET_PROPERTIES(${MODNAME} PROPERTIES XCODE_ATTRIBUTE_CC ${LLVM_ROOT_DIR}/bin/clang)
    SET_TARGET_PROPERTIES(${MODNAME} PROPERTIES XCODE_ATTRIBUTE_CXX ${LLVM_ROOT_DIR}/bin/clang++)
    SET_TARGET_PROPERTIES(${MODNAME} PROPERTIES XCODE_ATTRIBUTE_COMPILER_INDEX_STORE_ENABLE "No")
ENDIF()
INSTALL(TARGETS ${MODNAME} DESTINATION ${INSTALL_DIR})
IF (MSVC OR XCODE)
    SET_PROPERTY(TARGET ${MODNAME} PROPERTY FOLDER "modules/hydrology")
ENDIF ()
if(MSVC)
    foreach(flag_var
            CMAKE_CXX_FLAGS CMAKE_CXX_FLAGS_DEBUG CMAKE_CXX_FLAGS_RELEASE
            CMAKE_CXX_FLAGS_MINSIZEREL CMAKE_CXX_FLAGS_RELWITHDEBINFO)
        string(REGEX REPLACE "/MD" "/MT" ${flag_var} "${${flag_var}}")
    endforeach(flag_var)
endif()



